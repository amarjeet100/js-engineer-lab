# 🔤 JavaScript Strings

## 📌 Overview

This folder contains my practice and experiments with JavaScript string methods.

I am learning how to manipulate text, format values, clean user input, and use strings in real-world applications.

---

## 🧠 Major Topics Covered

* `slice()`
* 'join()'
* `replace()`
* `toUpperCase()`
* `toLowerCase()`
* `trim()`
* `split()`
* `includes()`
* `indexOf()`


---

## 💻 Files

### `strings.js`

Contains practice code, examples, and experiments for important JavaScript string methods.

---

## 🚀 Real-World Use Cases

These methods are useful for:

* Username formatting
* Email validation
* Password checking
* Search functionality
* Text formatting
* Data cleaning
* Word counting
* Input validation

---

## 🧪 Example

```js
let username = "   amarjeet   ";

console.log(username.trim().toUpperCase());
```

Output:

```js
AMARJEET
```

---

## 🎯 What I Learned

* Strings are immutable in JavaScript
* Most string methods return a new value
* String methods can be chained together
* Small methods can solve real-world problems
* Practice is important to build strong JavaScript fundamentals

---

## 📈 Goal

My goal is to master JavaScript deeply, build strong problem-solving skills, and create real-world projects using these concepts.
